data:
  data_dir: null
  train_split: train
  val_split: val
  max_train_samples: null
  max_val_samples: null

model:
  num_classes: 7
  pretrained: true

training:
  seed: 42
  epochs: 5
  batch_size: 4
  num_workers: 0
  device: auto
  output_dir: outputs/fasterrcnn_mobilenet_baseline

optimizer:
  lr: 0.005
  momentum: 0.9
  weight_decay: 0.0005