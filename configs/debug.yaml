data:
  data_dir: null
  train_split: train
  val_split: val
  max_train_samples: 16
  max_val_samples: 8

model:
  num_classes: 7
  pretrained: true

training:
  seed: 42
  epochs: 1
  batch_size: 2
  num_workers: 0
  device: auto
  output_dir: outputs/debug

optimizer:
  lr: 0.005
  momentum: 0.9
  weight_decay: 0.0005